# ARTales locked micro-symbol master

`artales-micro-symbol.master.v1.svg` is the locked vector source accepted for
the review-only icon artifact generator. Its companion JSON records the lock
and technical properties. Neither file is a runtime/public asset, and the lock
does not approve generated favicon or application icon deployment.

Changes require a new explicitly reviewed master version; do not overwrite v1.
